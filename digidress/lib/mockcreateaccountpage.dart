import 'package:flutter/material.dart';

class MockCreateAccountPage extends StatelessWidget {
  const MockCreateAccountPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Mock Create Account Page')),
    );
  }
}
